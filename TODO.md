* add different sampling strategies
* add pool of augmentations, eg. through Monai
* benchmark performance over different datasets with different chunk sizes and different batch sizes
* allow multiple bounding boxes per config volume
